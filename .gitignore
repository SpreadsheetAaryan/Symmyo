# ----------------------------------------
# 1. SENSITIVE DATA & API KEYS (CRITICAL)
# ----------------------------------------
# The .env file must NEVER be committed to Git as it contains your GEMINI_API_KEY.
.env
/venv